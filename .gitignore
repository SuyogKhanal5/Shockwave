token.txt
*.pyc
__pycache__/
.vscode/
.claude/
data/guildData/serverInfo/main.db
shockwave-site/
# MaxLinesFileHandler output (see bot.py) - a single line-capped log file.
shockwave.log*
# Daily main.db snapshots (see backupDatabaseTask in bot.py) - data, not
# source, same reasoning as main.db itself above.
data/guildData/backups/
# /preview's rendered cache (see PREVIEW_DIR/_cachedPreviewFiles in
# helper.py) - regenerated on demand from the logo/color-scheme/font/title
# catalogs already in source, so it's a build artifact, not source content.
assets/previews/