import type { Options } from "tsparticles-engine";
import type { RemoveMode } from "../../Types";
export declare type RemoveOptions = Options & {
    interactivity: {
        modes: RemoveMode;
    };
};
