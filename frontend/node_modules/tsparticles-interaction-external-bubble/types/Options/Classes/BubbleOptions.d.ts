import type { BubbleMode } from "../../Types";
import type { Options } from "tsparticles-engine";
export declare type BubbleOptions = Options & {
    interactivity: {
        modes: BubbleMode;
    };
};
