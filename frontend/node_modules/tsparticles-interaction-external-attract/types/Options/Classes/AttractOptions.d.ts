import type { AttractMode } from "../../Types";
import type { Options } from "tsparticles-engine";
export declare type AttractOptions = Options & {
    interactivity: {
        modes: AttractMode;
    };
};
