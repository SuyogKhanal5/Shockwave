import type { Options } from "tsparticles-engine";
import type { RepulseMode } from "../../Types";
export declare type RepulseOptions = Options & {
    interactivity: {
        modes: RepulseMode;
    };
};
